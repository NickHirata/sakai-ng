export interface Task {
    taskId?: number;
    creationDate?: Date;
    deadline?: Date;
    description?: string;
    // end_date?: Date;
    name?: string;
    // last_update?: Date;
    status?: string;
    // project_id?: number;
    // assigned_to?: number;
}
